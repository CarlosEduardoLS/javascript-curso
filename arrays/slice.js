let arr = [1, 2, 3, 4, 5]

// Função 'slice': Retorna um pedaço selecionado do array, recebe 2 índices, ou somente um índice de início
// e retorna um array somente com os valores selecionados.

let slice = arr.slice(2, 4)
console.log('SLICE -> ', slice)
