let arr = [4, 5, 10, 20, 35, 4, 5]

// Função 'lastIndexOf': Retorna o último indice em que o valor selecionado é encontrado no array, caso o valor não seja encontrado retorna -1.

const lastIndex = arr.lastIndexOf(5)
