let arr = [4, 5, 10, 20, 35, 4, 5]

// Função 'indexOf': Retorna o indice do primeiro valor selecionado encontrado no array, caso o valor não seja encontrado retorna -1.

const index = arr.indexOf(5)
